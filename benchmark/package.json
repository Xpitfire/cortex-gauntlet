{
  "name": "gauntlet-lint-toolchain",
  "private": true,
  "description": "Pinned JS/TS analyzers (tsc + eslint) the Gauntlet scorer runs over generated repos — the JS/TS analogue of the Python ruff/mypy toolchain in .venv. Run `npm install` here to activate eslint lint scoring (tsc also resolves from a global install).",
  "devDependencies": {
    "@eslint/js": "^9.17.0",
    "eslint": "^9.17.0",
    "typescript": "^5.7.0",
    "typescript-eslint": "^8.18.0"
  }
}
